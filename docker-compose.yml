services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: oidc_broker
    ports:
      - "5434:5432"
    volumes:
      - oidc-broker-pg:/var/lib/postgresql/data
volumes:
  oidc-broker-pg:

